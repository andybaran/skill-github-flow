To list your local branches:

```
git branch
```

Useful variations:

- `git branch` — lists local branches; the current one is marked with `*`.
- `git branch -v` — also shows the latest commit on each branch.
- `git branch -a` — lists both local and remote-tracking branches.
- `git branch -r` — lists only remote-tracking branches.
