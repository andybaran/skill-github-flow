To list your local branches:

```bash
git branch
```

The current branch is marked with an asterisk (`*`).

Handy variants:
- `git branch -v` — also show the latest commit on each branch
- `git branch -a` — include remote-tracking branches too
- `git branch -r` — remote branches only
- `git branch --show-current` — print just the name of the current branch
